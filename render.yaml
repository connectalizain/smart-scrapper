services:
  - type: web
    name: data-scraper
    env: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      playwright install chromium
    startCommand: |
      chainlit run app.py --host=0.0.0.0 --port=$PORT
